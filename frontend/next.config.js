/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // `swcMinify` was removed because Next.js warns about unrecognized options
  // If you need minification control, manage it via Next.js version or build tooling.
};

module.exports = nextConfig;
